# Laravel Lesson レビュー②

## Todo編集機能

### @method('PUT')を記述した行に何が出力されているか
- HTMLはpostとgetメソッドしか使用できない。そのため、@method('PUT')を使用することにより、
`<input type="hidden" name="_method" value="PUT">`として出力される。

### findメソッドの引数に指定しているIDは何のIDか
- find()メソッドの引数に指定しているIDは、データベースのidカラム（主キー）の値で、取得したいレコードを特定するためのID
### findメソッドで実行しているSQLは何か
- 主キー（id）を条件に1件取得するSQL
### findメソッドで取得できる値は何か
- 主キーに一致する1件のレコードをEloquentモデルとして取得する。該当データがなければ null を返す。
### saveメソッドは何を基準にINSERTとUPDATEを切り替えているのか
- save() メソッドは、モデルの exists プロパティ（データベースに存在するかどうか）を基準に、INSERT と UPDATE を切り替えている。
- exists = false → INSERT、exists = true → UPDATE
## Todo論理削除

### traitとclassの違いとは
- class はオブジェクトを生成するための設計図
- trait は複数のクラスで共通して使う機能を再利用するための仕組みで、単独ではインスタンス化できない。
### traitを使用するメリットとは
- traitを使用することで、複数のクラスで共通して使用する機能を再利用できる。重複したコードを書く必要がなくなり、保守性を向上させることができる。


## その他

### TodoControllerクラスのコンストラクタはどのタイミングで実行されるか
- Controllerのインスタンス生成時に自動的に実行される。LaravelではルーティングによってControllerが呼び出された際にコンストラクタが実行され、依存性注入によってTodoモデルを受け取っている。

### RequestクラスからFormRequestクラスに変更した理由
- バリデーション処理をコントローラーから分離し、役割を明確にしてコードの可読性・保守性を向上させるため
### $errorsのhasメソッドの引数・返り値は何か
- 引数:バリデーションエラーを確認する項目名（フィールド名）
- 戻り値：エラーが存在するかどうかを表す真偽値（true または false）
### $errorsのfirstメソッドの引数・返り値は何か
- 引数：エラーメッセージを取得したい項目名（フィールド名）

- 返り値：指定した項目の最初のエラーメッセージ（文字列）。エラーがない場合は空文字列
### フレームワークとは何か
- アプリケーション開発でよく使う機能や処理をあらかじめ用意し、開発を効率化するための土台（枠組み）のこと
### MVCはどういったアーキテクチャか
- MVCは、アプリケーションを「Model・View・Controller」の3つに分けて構成するアーキテクチャ（設計パターン）。
- Model（モデル）：データや業務処理を担当、View（ビュー）：画面表示を担当、Controller（コントローラー）：ユーザーからの入力を受け取り、ModelやViewを制御
### ORMとは何か、またLaravelが使用しているORMは何か
- ORMとは、プログラムのオブジェクトとデータベースのデータを対応付け、オブジェクト指向のコードでデータベースを操作できるようにする仕組み。LaravelではEloquent ORMを使用している。
### composer.json, composer.lockとは何か
- composer.json：プロジェクトで「どのパッケージが必要か」を定義するファイル
- composer.lock：実際にインストールする「パッケージの具体的なバージョン」を固定するファイル
### composerでインストールしたパッケージ（ライブラリ）はどのディレクトリに格納されるのか
‐ Composerでインストールしたパッケージは vendor ディレクトリに格納される。